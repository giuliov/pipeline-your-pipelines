trigger:
- master

pool:
  name: 'MakeAgents'
  demands:
  - Agent.OS -equals Linux
  - node.js

steps:
- script: |
    ng build --prod
  displayName: 'ng build'
  workingDirectory: 'src/test-apps/hello-world-ng'
