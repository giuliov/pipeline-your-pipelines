apiVersion: apps/v1
kind: Deployment
metadata:
  name: azdo-linux-agents-deployment
  labels:
    app: azdox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: azdox
  template:
    metadata:
      labels:
        app: azdox
    spec:
      containers:
      - name: azdo
        image: giuliovprodemo.azurecr.io/azure-pipelines-agent/linux/angular:latest
        env:
          - name: AZP_URL
            value: https://dev.azure.com/giuliovaad
          - name: AZP_TOKEN
            valueFrom:
              secretKeyRef:
                name: azurepipeline-token
                key: azurepipeline-token
          - name: AZP_POOL
            value: MakeAgents
      nodeSelector:
        kubernetes.io/os: linux



